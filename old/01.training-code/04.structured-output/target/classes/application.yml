spring:
  application:
    name: spring-ai-structured-output
  
  ai:
    openai:
      api-key: ${OPEN_AI_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 2000

server:
  port: 8080

logging:
  level:
    org.springframework.ai: DEBUG
