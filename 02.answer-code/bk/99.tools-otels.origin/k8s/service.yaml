apiVersion: v1
kind: Service
metadata:
  name: spring-ai-tools
  namespace: default
  labels:
    app: spring-ai-tools
spec:
  type: ClusterIP
  selector:
    app: spring-ai-tools
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
